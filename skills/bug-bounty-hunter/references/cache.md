# Cache behavior

Map cache headers, key inputs, CDN behavior, personalization, authorization variance, query normalization, and invalidation. Compare anonymous and authenticated responses using synthetic data and a single controlled key.

Do not poison shared caches or affect other users. Confirm a cache issue only when private or attacker-controlled content crosses a cache boundary and the behavior is reproducible under the engagement's safe test conditions.
