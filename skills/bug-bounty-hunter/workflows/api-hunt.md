# API hunt

Discover documented and client-referenced APIs, normalize routes, record methods/content types, and build an actor/object/function matrix. Test controls with synthetic objects, version and method differentials, field allowlists, pagination/filtering, and error behavior. Route GraphQL, JWT, uploads, SSRF, and business-state signals to their modules. Validate each candidate independently before reporting.
