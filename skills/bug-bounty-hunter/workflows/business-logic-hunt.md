# Business-logic hunt

Model the application's state machine and invariants. Identify transitions that can be skipped, repeated, reordered, replayed, or concurrently invoked. Use synthetic objects, dry-run paths, idempotency controls, and serial-vs-concurrent comparisons. Do not create real purchases, refunds, balances, or irreversible changes without explicit authorization.
